import styled from "styled-components";

const CourseListSection = styled.div`
  max-width: 350px;
  margin: auto;
  margin-top: 30px;
  overflow-x: visible;
  z-index: -1;
`;

const style = {
  CourseListSection,
};

export default style;
