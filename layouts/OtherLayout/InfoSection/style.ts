import styled from "styled-components";

const InfoSection = styled.div`
  max-width: 350px;
  height: 40px;
  margin: auto;
  margin-top: 10px;
  overflow: visible;
  position: sticky;
  top: 75px;
  z-index: 2;
`;

const style = {
  InfoSection,
};

export default style;
