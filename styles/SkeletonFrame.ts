import { css } from "styled-components";

export const SkeletonFrame = css`
  background-color: rgba(255, 255, 255, 0.1);
  box-sizing: border-box;
  box-shadow: 0px 0px 3px rgba(0, 0, 0, 0.3);
  border-radius: 10px;
  overflow: hidden;
`;
